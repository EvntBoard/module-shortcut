name: Module Google TTS

on:
  workflow_dispatch:
  push:
    tags:
    - "v*"


jobs:
  module-google-tts:
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v1

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v1
        with:
          node-version: 12

      - name: Install
        run: |
          cd module-google-tts
          yarn cache clean
          yarn install --network-timeout 1000000

      - name: Build
        run: |
          cd module-google-tts
          yarn build

      - name: Compress
        run: |
          cd module-google-tts/dist
          npx bestzip ../module-google-tts.zip ./*

      - name: Upload
        uses: bayssmekanique/action-simple-file-upload@v1
        with:
          user: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          host: ${{ secrets.FTP_HOST }}
          port: ${{ secrets.FTP_PORT }}
          src: ./module-google-tts/module-google-tts.zip
          dest: ./builds/${{ github.sha }}/modules/module-google-tts.zip